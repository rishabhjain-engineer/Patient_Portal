<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:dither="true"
    android:shape="rectangle"
    android:useLevel="false"
    android:visible="true" >

    <gradient
        android:endColor="@color/switch_back_ground"
        android:startColor="@color/switch_back_ground" />
    <size
        android:height="10dp"
        android:width="30dp" />

</shape>